<?xml version="1.0" encoding="utf-8" ?>
<commands>
  <command name="Group.Query" type="text" connection="local">
    <![CDATA[
  WITH  temp ( [ID], [ParentID], Name )
          AS ( SELECT   [ID] ,
                        [ParentID] ,
                        Name
               FROM     [dbo].[Group]
               WHERE    [ParentID] = @ID
               UNION ALL
               SELECT   G.[ID] ,
                        G.[ParentID] ,
                        G.Name
               FROM     [dbo].[Group] AS G
                        INNER JOIN temp ON G.[ParentID] = temp.[ID]
             )
    SELECT  *
    FROM    temp
      ]]>
  </command>
</commands>